# Kernel 
* kernel is a setof mathemtical functions used to take input and transform them into required form
* many kernels can be found, broadly categorised into linear and non linear kernels
* eg. linear, rbf, sigmoid, polynomial kernels

  ## svm in higher dimensions require hyperplane not line to categorise them
  * either projection and categorisation can be done or direct kernel trick can be used
  * projecting and then re projecting the points are very compute intensive
