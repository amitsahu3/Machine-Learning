# logistic regression to predict if a customer will buy an suv given his age and salary
* logistic regression is a regression algorithm for classfiying the categorically dependent variable 
* use of confusion matrix and accuracy scores to evaluate the model
